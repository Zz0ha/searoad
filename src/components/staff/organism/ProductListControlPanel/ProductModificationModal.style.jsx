import styled from 'styled-components/macro';

import colors from 'styles/variables/colors';

export const StTableProductEdit = styled.table`
  .field-name {
    padding: 10px 10px 10px 0;
  }
  .thumbnail-url {
    padding: 2px;
    border: 1px solid ${colors.borderColor};
    width: 100%;
  }
`;
